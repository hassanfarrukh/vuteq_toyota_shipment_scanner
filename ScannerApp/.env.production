NODE_ENV=production
# NEXT_PUBLIC_API_URL is intentionally NOT set
# client.ts uses smart port detection:
#   - Port 3000 (dev): Uses localhost:5000
#   - Port 80/443 (IIS): Uses same origin (window.location.origin)
PORT=3000
HOSTNAME=localhost
