# Author: Hassan
# Date: 2026-01-07
# Production Environment Variables Template
# Copy this file to .env.prod and update with actual values

# ===========================================
# SQL Server Configuration (Windows Host)
# ===========================================
DB_HOST=host.docker.internal
DB_PORT=1433
DB_NAME=VUTEQ_Scanner
DB_USER=VuteqApp
DB_PASSWORD=CHANGE_ME_SECURE_PASSWORD

# ===========================================
# ASP.NET Core Configuration
# ===========================================
ASPNETCORE_ENVIRONMENT=Production
ASPNETCORE_URLS=http://+:5000

# Connection String
CONNECTION_STRING=Server=host.docker.internal,1433;Database=VUTEQ_Scanner;User Id=VuteqApp;Password=CHANGE_ME;TrustServerCertificate=True;MultipleActiveResultSets=true

# ===========================================
# JWT Authentication
# ===========================================
JWT_SECRET=CHANGE_ME_MIN_32_CHARS_SECURE_KEY
JWT_ISSUER=VuteqScanner
JWT_AUDIENCE=VuteqScannerUsers
JWT_EXPIRY_MINUTES=60

# ===========================================
# Next.js Configuration
# ===========================================
NODE_ENV=production
NEXT_PUBLIC_API_URL=http://localhost/api
PORT=3000

# ===========================================
# Nginx Configuration
# ===========================================
NGINX_PORT=80

# ===========================================
# Security Notes
# ===========================================
# 1. NEVER commit .env.prod with real credentials
# 2. Use strong passwords (min 16 chars, mixed case, numbers, symbols)
# 3. JWT_SECRET must be at least 32 characters
# 4. Change all CHANGE_ME values before deployment
# 5. Restrict file permissions: chmod 600 .env.prod (Linux)
