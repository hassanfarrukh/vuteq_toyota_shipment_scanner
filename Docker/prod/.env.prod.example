# ==============================================================================
# VUTEQ Warehouse Scanning System - Production Environment Variables
# ==============================================================================
# Author: Hassan
# Date: 2025-10-20
# Description: Production environment configuration
#              Copy this file to .env.prod and update with ACTUAL production values
#              NEVER commit .env.prod to version control!
# ==============================================================================

# ==============================================================================
# BUILD CONFIGURATION
# ==============================================================================
APP_VERSION=1.0.0
BUILD_TIME=2025-10-20T00:00:00Z

# ==============================================================================
# DATABASE CONFIGURATION
# ==============================================================================
# CRITICAL: Use STRONG passwords (16+ characters, mixed case, numbers, symbols)
DB_SA_PASSWORD=CHANGE_THIS_TO_STRONG_PASSWORD_32_CHARS_MIN
DB_CONNECTION_STRING=Server=sqlserver;Database=VuteqWMS_Prod;User Id=sa;Password=CHANGE_THIS_TO_STRONG_PASSWORD_32_CHARS_MIN;TrustServerCertificate=True;MultipleActiveResultSets=true

# ==============================================================================
# JWT CONFIGURATION
# ==============================================================================
# CRITICAL: Generate a unique 32+ character secret key
# Example: openssl rand -base64 32
JWT_SECRET=CHANGE_THIS_TO_RANDOM_SECRET_KEY_32_CHARS_MIN
JWT_ISSUER=VuteqWMS
JWT_AUDIENCE=VuteqClients
JWT_EXPIRY_MINUTES=60

# ==============================================================================
# TOYOTA API CONFIGURATION - PRODUCTION ENVIRONMENT
# ==============================================================================
TOYOTA_API_PROD_URL=https://api.toyota.com
TOYOTA_API_PROD_CLIENT_ID=YOUR_PRODUCTION_CLIENT_ID
TOYOTA_API_PROD_CLIENT_SECRET=YOUR_PRODUCTION_CLIENT_SECRET

# ==============================================================================
# TOYOTA API CONFIGURATION - QA ENVIRONMENT (FALLBACK)
# ==============================================================================
TOYOTA_API_QA_URL=https://qa-api.toyota.com
TOYOTA_API_QA_CLIENT_ID=YOUR_QA_CLIENT_ID
TOYOTA_API_QA_CLIENT_SECRET=YOUR_QA_CLIENT_SECRET

# ==============================================================================
# MULTI-SITE CONFIGURATION
# ==============================================================================
DEFAULT_SITE_CODE=TMH

# ==============================================================================
# NEXT.JS CONFIGURATION
# ==============================================================================
# Update with your actual production domain
NEXT_PUBLIC_API_URL=https://yourdomain.com/api

# ==============================================================================
# CORS CONFIGURATION
# ==============================================================================
# Update with your actual production domain
CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# ==============================================================================
# SECURITY NOTES
# ==============================================================================
# 1. NEVER use default passwords in production
# 2. NEVER commit .env.prod to version control
# 3. Rotate secrets every 90 days
# 4. Use password manager or secrets vault (Azure Key Vault, AWS Secrets Manager)
# 5. Restrict access to .env.prod file (chmod 600)
# 6. Enable SSL/TLS for all external communications
# 7. Use strong passwords: minimum 16 characters, mixed case, numbers, symbols
#
# Generate strong passwords:
#   Linux/Mac: openssl rand -base64 32
#   PowerShell: -join ((48..57) + (65..90) + (97..122) | Get-Random -Count 32 | % {[char]$_})
#
# ==============================================================================
# DEPLOYMENT CHECKLIST
# ==============================================================================
# [ ] Updated all passwords to production values
# [ ] Updated JWT secret to unique random value
# [ ] Updated Toyota API credentials (Production)
# [ ] Updated NEXT_PUBLIC_API_URL with production domain
# [ ] Updated CORS_ALLOWED_ORIGINS with production domain
# [ ] Verified SSL certificates are in place
# [ ] Tested database backup procedures
# [ ] Configured monitoring and alerting
# [ ] Reviewed security best practices
# [ ] Set file permissions: chmod 600 .env.prod
# ==============================================================================
